import SignoutAll from "@/components/dashboard/settings/security/SignoutAll";

export default function Security() {
  return (
    <section className="grid gap-8">
      <SignoutAll />
    </section>
  );
}
